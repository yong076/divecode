name: security
title: Application security (auth, authz, secrets, OWASP)
triggers: oauth, jwt, sso, csrf, xss, sql injection, pii, gdpr, hipaa, pci, soc2, authz, rbac, secrets, csp, hsts, idor, session, password, hash, encryption
severity_default: high
