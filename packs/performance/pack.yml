name: performance
title: Performance / latency / throughput patterns
triggers: latency, p99, throughput, load test, web vitals, lighthouse, lcp, inp, cls, n+1, cdn, bundle size, slow query, hot path, cold start
severity_default: medium
