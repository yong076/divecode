# slice-plan.md

> Generated by divecode-slice-plan. Profile-conditional fields below; remove the ones not required for your profile.

Bolt: `__BOLT_ID__` · Profile: `__PROFILE__` · Size: `__SIZE__`

---

## Slice 1 — <name>

**Goal**: <one sentence in ubiquitous language from design.md §3>

<!-- light fields end here. Standard+ adds: -->

**Layer scope**: domain | usecase | data | presentation (or combination)

**Aggregates touched**: <from design.md §3>

**Test cases (drive RED)**:
- `test_name_1`   _(strict: must cite acceptance criterion from design.md §2)_
- `test_name_2`

**Files expected to change**:
- `path/to/file.ext`

**Verification command**: `<exact bash to run after GREEN>`

<!-- strict-only: -->
**Repository Pattern note** (only if slice touches data layer): <how this slice satisfies the Google Repository Pattern per design.md §4>

---

## Slice 2 — ...

(repeat per slice)

---

## Order rationale

(brief explanation of why this order — what depends on what, what was deliberately deferred)

## Pause

⏸  **Chain pauses here.** Reviewer must read both `divecode/design.md` and this `divecode/slice-plan.md` before invoking `divecode-worktree`.

Reviewer should answer:
1. Does design.md capture all decisions accurately?
2. Are the slices the right cut (split / merge needed)?
3. Is the order right (dependencies missed)?
4. Any slice that should be NO-GO before Slice 1 starts?
